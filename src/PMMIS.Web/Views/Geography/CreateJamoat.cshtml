@model PMMIS.Domain.Entities.Jamoat
@{
    var district = ViewBag.District as PMMIS.Domain.Entities.District;
    ViewData["Title"] = "Добавить джамоат";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <nav aria-label="breadcrumb" class="mb-4">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-action="Index">География</a></li>
            <li class="breadcrumb-item"><a asp-action="Jamoats" asp-route-districtId="@district?.Id">@district?.NameRu</a></li>
            <li class="breadcrumb-item active">Добавить джамоат</li>
        </ol>
    </nav>

    <div class="row">
        <div class="col-lg-8">
            <div class="card border-0 shadow-sm">
                <div class="card-header bg-white">
                    <h5 class="card-title mb-0"><i class="bi bi-plus-circle me-2"></i>Новый джамоат</h5>
                </div>
                <div class="card-body">
                    <form asp-action="CreateJamoat" method="post">
                        <input type="hidden" asp-for="DistrictId" />
                        
                        <div class="row g-3">
                            <div class="col-md-6">
                                <label class="form-label">Район</label>
                                <input type="text" class="form-control" value="@district?.NameRu" disabled />
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">Код <span class="text-danger">*</span></label>
                                <input asp-for="Code" class="form-control" placeholder="JAM01" required />
                            </div>
                        </div>

                        <hr class="my-4" />
                        <h6 class="text-muted mb-3"><i class="bi bi-translate me-2"></i>Локализация названия</h6>

                        <div class="row g-3">
                            <div class="col-md-4">
                                <label class="form-label">🇷🇺 Русский <span class="text-danger">*</span></label>
                                <input asp-for="NameRu" class="form-control" required />
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">🇹🇯 Тоҷикӣ <span class="text-danger">*</span></label>
                                <input asp-for="NameTj" class="form-control" required />
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">🇺🇸 English <span class="text-danger">*</span></label>
                                <input asp-for="NameEn" class="form-control" required />
                            </div>
                        </div>

                        <hr class="my-4" />

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-check-lg me-1"></i> Сохранить
                            </button>
                            <a asp-action="Jamoats" asp-route-districtId="@district?.Id" class="btn btn-outline-secondary">Отмена</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
