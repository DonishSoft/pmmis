@model PMMIS.Web.ViewModels.Roles.RoleFormViewModel
@{
    ViewData["Title"] = "Добавить роль";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <nav aria-label="breadcrumb" class="mb-4">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-action="Index">Роли</a></li>
            <li class="breadcrumb-item active">Добавить роль</li>
        </ol>
    </nav>

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show">
            <i class="bi bi-exclamation-circle me-2"></i>@TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <div class="row">
        <div class="col-lg-6">
            <form asp-action="Create" method="post">
                <div class="card border-0 shadow-sm mb-4">
                    <div class="card-header bg-white">
                        <h5 class="mb-0"><i class="bi bi-shield-plus me-2"></i>Новая роль</h5>
                    </div>
                    <div class="card-body">
                        <div class="mb-3">
                            <label class="form-label">Название роли <span class="text-danger">*</span></label>
                            <input asp-for="Role.Name" class="form-control" placeholder="ROLE_NAME" required />
                            <small class="text-muted">Будет преобразовано в верхний регистр</small>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">№ сортировки</label>
                            <input asp-for="Role.SortOrder" type="number" class="form-control" value="0" />
                        </div>
                        
                        <hr class="my-4" />
                        <h6 class="text-muted mb-3"><i class="bi bi-translate me-2"></i>Описание</h6>
                        
                        <div class="mb-3">
                            <label class="form-label">🇷🇺 Русский</label>
                            <textarea asp-for="Role.Description" class="form-control" rows="2" placeholder="Описание роли..."></textarea>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">🇹🇯 Тоҷикӣ</label>
                            <textarea asp-for="Role.DescriptionTj" class="form-control" rows="2"></textarea>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">🇺🇸 English</label>
                            <textarea asp-for="Role.DescriptionEn" class="form-control" rows="2"></textarea>
                        </div>
                    </div>
                </div>

                <div class="card border-0 shadow-sm mb-4">
                    <div class="card-header bg-white">
                        <h5 class="mb-0"><i class="bi bi-menu-button-wide me-2"></i>Доступ к меню</h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            @foreach (var item in Model.MenuKeys)
                            {
                                <div class="col-md-6 mb-2">
                                    <div class="form-check">
                                        <input type="checkbox" name="permissions" value="@item.Key" class="form-check-input" id="perm_@item.Key" />
                                        <label class="form-check-label" for="perm_@item.Key">
                                            @item.Value
                                        </label>
                                    </div>
                                </div>
                            }
                        </div>
                        <div class="mt-3">
                            <button type="button" class="btn btn-sm btn-outline-primary" onclick="selectAll(true)">Выбрать все</button>
                            <button type="button" class="btn btn-sm btn-outline-secondary" onclick="selectAll(false)">Снять все</button>
                        </div>
                    </div>
                </div>

                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-check-lg me-1"></i> Создать
                    </button>
                    <a asp-action="Index" class="btn btn-outline-secondary">Отмена</a>
                </div>
            </form>
        </div>
    </div>
</div>

<script>
function selectAll(checked) {
    document.querySelectorAll('input[name="permissions"]').forEach(cb => cb.checked = checked);
}
</script>
