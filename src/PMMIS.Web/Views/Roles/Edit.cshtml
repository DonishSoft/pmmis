@model PMMIS.Web.ViewModels.Roles.RoleFormViewModel
@{
    ViewData["Title"] = "Редактировать роль";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var role = Model.Role;
}

<div class="container-fluid">
    <nav aria-label="breadcrumb" class="mb-4">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-action="Index">Роли</a></li>
            <li class="breadcrumb-item active">@role.Name</li>
        </ol>
    </nav>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show">
            <i class="bi bi-check-circle me-2"></i>@TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show">
            <i class="bi bi-exclamation-circle me-2"></i>@TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <div class="row">
        <div class="col-lg-7">
            <form asp-action="Edit" method="post">
                <input type="hidden" name="id" value="@role.Id" />
                
                <div class="card border-0 shadow-sm mb-4">
                    <div class="card-header bg-white d-flex justify-content-between align-items-center">
                        <h5 class="mb-0"><i class="bi bi-shield me-2"></i>@role.Name</h5>
                        @if (role.IsSystem)
                        {
                            <span class="badge bg-dark"><i class="bi bi-lock"></i> Системная</span>
                        }
                    </div>
                    <div class="card-body">
                        @if (!role.IsSystem)
                        {
                            <div class="mb-3">
                                <label class="form-label">Название роли</label>
                                <input asp-for="Role.Name" class="form-control" />
                            </div>
                        }
                        
                        <div class="mb-3">
                            <label class="form-label">№ сортировки</label>
                            <input asp-for="Role.SortOrder" type="number" class="form-control" />
                        </div>
                        
                        <hr class="my-4" />
                        <h6 class="text-muted mb-3"><i class="bi bi-translate me-2"></i>Описание</h6>
                        
                        <div class="mb-3">
                            <label class="form-label">🇷🇺 Русский</label>
                            <textarea asp-for="Role.Description" class="form-control" rows="2"></textarea>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">🇹🇯 Тоҷикӣ</label>
                            <textarea asp-for="Role.DescriptionTj" class="form-control" rows="2"></textarea>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">🇺🇸 English</label>
                            <textarea asp-for="Role.DescriptionEn" class="form-control" rows="2"></textarea>
                        </div>
                    </div>
                </div>

                <div class="card border-0 shadow-sm mb-4">
                    <div class="card-header bg-white">
                        <h5 class="mb-0"><i class="bi bi-menu-button-wide me-2"></i>Доступ к меню</h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            @foreach (var item in Model.MenuKeys)
                            {
                                var isChecked = false;
                                <div class="col-md-6 mb-2">
                                    <div class="form-check">
                                        <input type="checkbox" name="permissions" value="@item.Key" class="form-check-input" id="perm_@item.Key" @(isChecked ? "checked" : "") />
                                        <label class="form-check-label" for="perm_@item.Key">
                                            @item.Value
                                        </label>
                                    </div>
                                </div>
                            }
                        </div>
                        <div class="mt-3">
                            <button type="button" class="btn btn-sm btn-outline-primary" onclick="selectAll(true)">Выбрать все</button>
                            <button type="button" class="btn btn-sm btn-outline-secondary" onclick="selectAll(false)">Снять все</button>
                        </div>
                    </div>
                </div>

                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-check-lg me-1"></i> Сохранить
                    </button>
                    <a asp-action="Index" class="btn btn-outline-secondary">Назад</a>
                </div>
            </form>
            
            @if (!role.IsSystem && Model.UsersInRole.Count == 0)
            {
                <div class="mt-4">
                    <form asp-action="Delete" asp-route-id="@role.Id" method="post" onsubmit="return confirm('Удалить роль?')">
                        <button type="submit" class="btn btn-outline-danger btn-sm">
                            <i class="bi bi-trash me-1"></i> Удалить роль
                        </button>
                    </form>
                </div>
            }
        </div>

        <div class="col-lg-5">
            <div class="card border-0 shadow-sm">
                <div class="card-header bg-light">
                    <h6 class="mb-0"><i class="bi bi-people me-2"></i>Пользователи с этой ролью (@Model.UsersInRole.Count)</h6>
                </div>
                <div class="card-body p-0">
                    @if (Model.UsersInRole.Any())
                    {
                        <ul class="list-group list-group-flush">
                            @foreach (var user in Model.UsersInRole)
                            {
                                <li class="list-group-item d-flex justify-content-between align-items-center">
                                    <div class="d-flex align-items-center">
                                        @if (!string.IsNullOrEmpty(user.PhotoPath))
                                        {
                                            <img src="@user.PhotoPath" class="rounded-circle me-2" style="width: 32px; height: 32px; object-fit: cover;" />
                                        }
                                        else
                                        {
                                            <div class="rounded-circle bg-secondary d-flex align-items-center justify-content-center text-white me-2" style="width: 32px; height: 32px; font-size: 12px;">
                                                @user.FirstName.Substring(0, 1)@user.LastName.Substring(0, 1)
                                            </div>
                                        }
                                        <div>
                                            <strong>@user.FullName</strong>
                                            <br><small class="text-muted">@user.Email</small>
                                        </div>
                                    </div>
                                    <a asp-controller="Users" asp-action="Edit" asp-route-id="@user.Id" class="btn btn-sm btn-outline-primary">
                                        <i class="bi bi-pencil"></i>
                                    </a>
                                </li>
                            }
                        </ul>
                    }
                    else
                    {
                        <div class="text-center text-muted py-4">
                            <i class="bi bi-person-x fs-3 d-block mb-2"></i>
                            Нет пользователей с этой ролью
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

<script>
function selectAll(checked) {
    document.querySelectorAll('input[name="permissions"]').forEach(cb => cb.checked = checked);
}
</script>
