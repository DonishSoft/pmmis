@model PMMIS.Domain.Entities.School
@{
    var village = ViewBag.Village as PMMIS.Domain.Entities.Village;
    var types = ViewBag.EducationTypes as List<PMMIS.Domain.Entities.EducationInstitutionType>;
    ViewData["Title"] = "Редактировать школу";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <nav aria-label="breadcrumb" class="mb-4">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-action="Index">География</a></li>
            <li class="breadcrumb-item"><a asp-action="VillageDetails" asp-route-id="@village?.Id">@village?.NameRu</a></li>
            <li class="breadcrumb-item active">Редактировать школу</li>
        </ol>
    </nav>

    <div class="row">
        <div class="col-lg-8">
            <div class="card border-0 shadow-sm">
                <div class="card-header bg-white d-flex justify-content-between align-items-center">
                    <h5 class="card-title mb-0"><i class="bi bi-pencil me-2"></i>Школа №@Model.Number</h5>
                    <form asp-action="DeleteSchool" asp-route-id="@Model.Id" method="post" onsubmit="return confirm('Удалить эту школу?');">
                        <button type="submit" class="btn btn-sm btn-outline-danger">
                            <i class="bi bi-trash"></i> Удалить
                        </button>
                    </form>
                </div>
                <div class="card-body">
                    <form asp-action="EditSchool" method="post">
                        <input type="hidden" asp-for="Id" />
                        <input type="hidden" asp-for="VillageId" />

                        <div class="row g-3">
                            <div class="col-md-4">
                                <label class="form-label">Тип учреждения</label>
                                <select asp-for="TypeId" class="form-select">
                                    <option value="">-- Выберите тип --</option>
                                    @if (types != null)
                                    {
                                        @foreach (var t in types.Where(x => x.IsActive).OrderBy(x => x.SortOrder))
                                        {
                                            <option value="@t.Id" selected="@(Model.TypeId == t.Id)">@t.Name</option>
                                        }
                                    }
                                </select>
                            </div>
                            <div class="col-md-2">
                                <label class="form-label">№ школы <span class="text-danger">*</span></label>
                                <input asp-for="Number" type="number" class="form-control" required />
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">Название</label>
                                <input asp-for="Name" class="form-control" placeholder="Необязательно" />
                            </div>
                        </div>

                        <hr class="my-4" />
                        <h6 class="text-muted mb-3"><i class="bi bi-bar-chart me-2"></i>Статистика</h6>

                        <div class="row g-3">
                            <div class="col-md-3">
                                <label class="form-label">Количество учеников</label>
                                <input asp-for="TotalStudents" type="number" class="form-control" min="0" />
                            </div>
                            <div class="col-md-3">
                                <label class="form-label">Из них учениц</label>
                                <input asp-for="FemaleStudents" type="number" class="form-control" min="0" />
                            </div>
                            <div class="col-md-3">
                                <label class="form-label">Количество учителей</label>
                                <input asp-for="TeachersCount" type="number" class="form-control" min="0" />
                            </div>
                            <div class="col-md-3">
                                <label class="form-label">Учителей-женщин</label>
                                <input asp-for="FemaleTeachersCount" type="number" class="form-control" min="0" />
                            </div>
                        </div>

                        <hr class="my-4" />
                        <h6 class="text-muted mb-3"><i class="bi bi-droplet me-2"></i>Инфраструктура ВКХ</h6>

                        <div class="row g-3">
                            <div class="col-md-6">
                                <div class="form-check form-switch">
                                    <input asp-for="HasWaterSupply" class="form-check-input" type="checkbox" />
                                    <label class="form-check-label">💧 Есть водоснабжение</label>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-check form-switch">
                                    <input asp-for="HasSanitation" class="form-check-input" type="checkbox" />
                                    <label class="form-check-label">🚿 Есть санитарные условия</label>
                                </div>
                            </div>
                        </div>

                        <hr class="my-4" />
                        
                        <div class="mb-3">
                            <label class="form-label">Примечания</label>
                            <textarea asp-for="Notes" class="form-control" rows="3"></textarea>
                        </div>

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-check-lg me-1"></i> Сохранить
                            </button>
                            <a asp-action="VillageDetails" asp-route-id="@village?.Id" class="btn btn-outline-secondary">Отмена</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
